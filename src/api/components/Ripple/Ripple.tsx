/**
 * Ripple — visual click ripple effect. Pure re-export of HeroUI's
 * `Ripple`. Atlas's flat-design defaults disable button animation by
 * default; use `Ripple` explicitly only where a touch ripple genuinely
 * improves the affordance (e.g. inside custom press surfaces).
 *
 * Ported from `Xeekrsmainapp/src/components/heroui-branded/misc.tsx`.
 */
export { Ripple } from "@heroui/react";
