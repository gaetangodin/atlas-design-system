/**
 * ScrollShadow — HeroUI native pass-through. Adds soft fades at the
 * scroll edges to indicate overflow. No brand override needed.
 */
import { ScrollShadow as HeroUIScrollShadow, type ScrollShadowProps } from "@heroui/react";

export type { ScrollShadowProps };
export const ScrollShadow = HeroUIScrollShadow;
